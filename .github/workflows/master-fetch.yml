name: master-fetch

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      app: master-fetch
      upstream: dondai1234/master-fetch
      # platforms: linux/amd64,linux/arm64   # uncomment for multi-arch (extra CI minutes)
