name: donsetch

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      app: donsetch
      upstream: dondai44423/donsetch
      # platforms: linux/amd64,linux/arm64   # uncomment for multi-arch (extra CI minutes)
