name: donsetch

on:
  schedule:
    - cron: "37 4 * * 1"   # Mondays 04:37 UTC
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      app: donsetch
      upstream: dondai44423/donsetch
